const cell = document.querySelectorAll('.gameboard>div');

const gameBoard = (() => {
let playField = 
['','','',
'','','',
'','','',]
return {playField,};
})();

const displayController = (() => {

})();

const player = (name) => {
    const placeMark = () => {};
    return {
        placeMark,
    }
}

